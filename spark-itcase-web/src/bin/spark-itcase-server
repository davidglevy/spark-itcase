#!/bin/bash

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
java -DCONF_DIR=${DIR}/../conf -DLOG_DIR=${DIR}/../log -jar ${DIR}/../spark-itcase.jar solutions.deepfield.spark.itcase.web.server.EmbeddedJetty $1
